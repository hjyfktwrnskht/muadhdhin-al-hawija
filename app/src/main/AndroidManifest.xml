<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- الأذونات المطلوبة للعمل في الخلفية وتشغيل الأذان -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" /> <!-- للتحقق من الوقت عبر الإنترنت إذا لزم الأمر، أو للتحديثات المستقبلية -->
    
    <!-- أذونات الإشعارات (لأندرويد 13 فما فوق) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- أذونات لجدولة التنبيهات الدقيقة (Exact Alarms) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- طلب تجاهل تحسينات البطارية (للتأكد من استمرار عمل التطبيق) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MuadhdhinAlHawija"
        tools:targetApi="31">

        <!-- النشاط الرئيسي -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- خدمة Foreground Service لتشغيل الأذان في الخلفية -->
        <service
            android:name=".AdhanService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- مستقبل (Receiver) للتشغيل التلقائي بعد إعادة التشغيل -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- مستقبل (Receiver) لإيقاف الأذان من الإشعار -->
        <receiver
            android:name=".AdhanStopReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- تعريف الـ Widget -->
        <receiver android:name=".AdhanAppWidgetProvider" android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/adhan_app_widget_info" />
        </receiver>

    </application>
</manifest>
