# 📱 دليل بناء APK باستخدام Termux على الهاتف

هذا الدليل يشرح كيفية بناء ملف APK لتطبيق **مؤذّن الحويجة** مباشرة من هاتفك الأندرويد باستخدام **Termux** بدون الحاجة لكمبيوتر.

---

## ✅ المتطلبات

- **هاتف أندرويد** (API 24 أو أعلى)
- **تطبيق Termux** (مجاني من Google Play)
- **اتصال إنترنت** (لتحميل الأدوات والمكتبات)
- **مساحة تخزين** (حوالي 2-3 GB)

---

## 🚀 خطوات البناء

### **الخطوة 1: تحميل وتثبيت Termux**

1. افتح **Google Play Store** على هاتفك
2. ابحث عن **"Termux"**
3. اضغط على **"تثبيت"** (Install)
4. انتظر حتى ينتهي التثبيت

**ملاحظة:** تأكد من أن لديك مساحة تخزين كافية (حوالي 3 GB على الأقل)

---

### **الخطوة 2: فتح Termux والتحضير**

1. افتح تطبيق **Termux**
2. سيظهر لك سطر الأوامر (Terminal)
3. انسخ والصق الأوامر التالية واحدة تلو الأخرى:

```bash
# تحديث قوائم الحزم
apt update && apt upgrade -y

# تثبيت الأدوات الأساسية
apt install -y git openjdk-17 wget unzip

# إنشاء مجلد للمشروع
mkdir -p ~/projects
cd ~/projects
```

**ملاحظة:** قد يستغرق هذا 10-15 دقيقة حسب سرعة الإنترنت

---

### **الخطوة 3: تحميل المشروع**

اختر **أحد الخيارين** التالية:

#### **الخيار أ: تحميل من GitHub (الأفضل)**

```bash
# استنساخ المستودع
git clone https://github.com/hjyfktwrnskht/muadhdhin-al-hawija.git
cd muadhdhin-al-hawija
```

#### **الخيار ب: تحميل ملف ZIP**

إذا كان لديك ملف ZIP للمشروع:

```bash
# تحميل الملف (استبدل الرابط بالرابط الفعلي)
wget https://your-download-link.zip -O muadhdhin.zip

# فك الضغط
unzip muadhdhin.zip
cd muadhdhin_al_hawija
```

---

### **الخطوة 4: بناء APK**

الآن نأتي للخطوة الأهم - بناء ملف APK:

```bash
# جعل gradlew قابل للتنفيذ
chmod +x ./gradlew

# بناء APK (هذه قد تستغرق 10-20 دقيقة)
./gradlew assembleDebug
```

**ملاحظة:** قد تظهر رسائل تحذير - لا تقلق، هذا طبيعي. الأهم أن تنتهي بدون أخطاء.

---

### **الخطوة 5: تحميل ملف APK**

بعد انتهاء البناء بنجاح، ستجد ملف APK هنا:

```bash
# عرض المسار الكامل
ls -la app/build/outputs/apk/debug/app-debug.apk
```

**المسار:** `~/projects/muadhdhin_al_hawija/app/build/outputs/apk/debug/app-debug.apk`

---

## 📥 نقل ملف APK إلى مجلد التحميلات

لسهولة الوصول إلى الملف:

```bash
# نسخ الملف إلى مجلد التحميلات
cp app/build/outputs/apk/debug/app-debug.apk ~/storage/downloads/muadhdhin-al-hawija.apk

# التحقق من وجود الملف
ls -la ~/storage/downloads/muadhdhin-al-hawija.apk
```

الآن يمكنك الذهاب إلى **ملف التحميلات** على هاتفك وستجد الملف هناك!

---

## 🔧 استكشاف الأخطاء

### **إذا واجهت خطأ في البناء:**

```bash
# حذف الملفات المؤقتة وإعادة المحاولة
./gradlew clean
./gradlew assembleDebug
```

### **إذا استهلك البناء ذاكرة كبيرة:**

```bash
# بناء بمتطلبات ذاكرة أقل
./gradlew assembleDebug -Dorg.gradle.jvmargs="-Xmx512m"
```

### **إذا كان الإنترنت بطيء:**

```bash
# تحميل المكتبات بشكل منفصل أولاً
./gradlew build --offline
```

---

## 📱 تثبيت APK على الهاتف

بعد بناء APK بنجاح:

1. افتح **ملف التحميلات** (Files/Downloads)
2. ابحث عن **muadhdhin-al-hawija.apk**
3. اضغط عليه لتثبيت التطبيق
4. اتبع التعليمات على الشاشة

---

## ⏱️ الوقت المتوقع

- **تثبيت الأدوات:** 15-20 دقيقة
- **تحميل المشروع:** 2-5 دقائق
- **بناء APK:** 10-20 دقيقة
- **المجموع:** 30-45 دقيقة (حسب سرعة الهاتف والإنترنت)

---

## 💡 نصائح مهمة

1. **استخدم WiFi** بدلاً من الإنترنت المحمول (أسرع وأوفر)
2. **أغلق التطبيقات الأخرى** أثناء البناء (لتوفير الذاكرة)
3. **لا تطفئ الشاشة** أثناء البناء (قد يتوقف البناء)
4. **تأكد من شحن البطارية** (على الأقل 50%)

---

## ❓ أسئلة شائعة

**س: هل يمكن بناء APK بدون إنترنت بعد التثبيت الأول؟**
ج: لا، تحتاج إنترنت في كل بناء لتحميل المكتبات.

**س: كم مساحة تخزين يحتاج التطبيق؟**
ج: حوالي 2-3 GB للأدوات والمكتبات، و 50 MB لملف APK النهائي.

**س: هل يمكن استخدام Termux على أي هاتف أندرويد؟**
ج: نعم، لكن يفضل هاتف حديث نسبياً (API 24 أو أعلى).

**س: ماذا لو فشل البناء؟**
ج: جرب `./gradlew clean` ثم أعد المحاولة.

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تأكد من اتباع الخطوات بالترتيب
2. تأكد من وجود مساحة تخزين كافية
3. جرب إعادة تشغيل Termux والهاتف

---

**تم إنشاء هذا الدليل خصيصاً لتطبيق مؤذّن الحويجة**
© 2025 - جميع الحقوق محفوظة
